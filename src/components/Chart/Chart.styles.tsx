import { Chart as MultitypeChart } from 'react-chartjs-2';
import styled from 'styled-components';

const Chart = styled(MultitypeChart)`
  width: 100% !important;
`;

const Styled = {
  Chart,
};

export default Styled;
